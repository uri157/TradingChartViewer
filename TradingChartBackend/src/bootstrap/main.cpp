#include <cstdio>

int main(int argc, char** argv) {
    // TODO(api): init config/env
    // TODO(api): init duckdb connection / migration
    // TODO(api): start HTTP server
    // temporal: imprime y sale con 0 si no está la API implementada aún
    std::puts("Backend-only build: UI removed. API server entrypoint goes here.");
    return 0;
}
